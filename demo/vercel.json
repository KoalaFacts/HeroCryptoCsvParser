{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "installCommand": "cd .. && npm install && npm run build && cd demo && npm install",
  "devCommand": "npm run dev",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
